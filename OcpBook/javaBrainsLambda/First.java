package javaBrainsLambda;

public class First {
	/*
	 * why funcitonal programming
	 * it is  more readable code so less maintaing
	 * it is another paradigm
	 * 
	 * a lamda expression means that we us function as a variable
	 * 
	 * like aBlockOfCode=public void funtionName(){
	 * 									System.out.println("hello world	");
	 * 											}
	 * 
	 * in a simple program it gives   aBlockOfCode= ()->{
	 * 														System.out.println("");
	 * 													}
	 * only a simple addition or     ()-> Sysout(); 
	 * 
	 * 
	 * or we can use inline lamda expression like
	 * 
	 *  
	 */
	
	void perform(int i) {
		
		
	}
	
	 abstract class Enum<E extends Enum <E>> implements  Comparable<E>, Serializable {

			// Rube Goldberg creator of rube goldberg
			// example back scratcher
			
			//simple is not terse
			// simple keeps you focused
			// saving the yak
			
			// simple keep you focused
			
			
			// learn membe reference (::)
		}

	public static void main(String...strings) {
		 
		// aBlockOfCode= ()->
		 {
			 System.out.println("a");
		 }
		 
		 
		 First f = new First();
		 
		 f.perform(44);
		 
	//	 f.perform( ()->System.out.println("hello world")  );
		 
	// funtion for string length found by lambda expression	 
	//	 stringLengthCountfunction=(String str)->str.length();
		 
	}
	
}
